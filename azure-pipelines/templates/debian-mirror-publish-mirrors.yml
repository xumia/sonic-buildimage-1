parameters:
- name: 'mirrorNames'
  type: object
  default:
  - jessie-security
  - stretch-security
  - buster-security
jobs:
- template: debian-mirror-publish-mirrors-internal.yml
  parameters:
    mirrorNames: ${{ parameters.mirrorNames }}

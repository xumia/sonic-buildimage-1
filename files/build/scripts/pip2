#!/bin/bash

PARENT_PATH=$(dirname $0)

PIP_COMMAND=/usr/bin/pip2
[ -x /usr/local/bin/pip2 ] && PIP_COMMAND=/usr/local/bin/pip2

if [ ! -x "$PIP_COMMAND" ]; then
    echo "The command pip2 not found" 1>&2
    exit 1
fi

PIP_COMPONENT=py2 PIP_COMMAND="$PIP_COMMAND" ${PARENT_PATH}/pip $@

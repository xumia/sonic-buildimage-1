NS="asic" #Network namespace
[ "$1" -eq "$1" ] 2> /dev/null # check if the first argument is a number
if [ $? -eq 0 ]; then
    DEV="$1"
    shift
fi
if [ -z "$DEV" ]; then
    /usr/local/bin/sonic-cfggen "$@"
else
    ip netns exec $NS$DEV /usr/local/bin/sonic-cfggen "$@"
fi
